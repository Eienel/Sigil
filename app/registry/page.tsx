import { PageStub } from "@/components/page-stub";

export default function RegistryPage() {
  return (
    <PageStub
      phase="Registry"
      title="Recent attestations"
      body="A live feed of recent Sigil attestations read from Sui events through Tatum. Built alongside the chain flows."
    />
  );
}
