import { PageStub } from "@/components/page-stub";

export default function AppPage() {
  return (
    <PageStub
      phase="Sign"
      title="Sign with your wallet"
      body="The human sign flow lands here. Connect a wallet, drop a file, choose a provenance type, and press your seal. Built in Phase 4."
    />
  );
}
